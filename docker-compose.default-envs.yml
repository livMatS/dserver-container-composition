---
version: '3.8'

services:
  mongodb:
    env_file:
      - ./envs/production/mongodb

  postgres:
    env_file:
      - ./envs/production/postgres

  dserver:
    env_file:
      - ./envs/production/dserver

  token_generator_ldap:
    env_file:
      - ./envs/production/token_generator_ldap
